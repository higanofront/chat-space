
.side
  .side__side-header
    .side__side-header__side-box
      = current_user.name
    .side__side-header__side-icon
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog')
      = link_to new_group_path, class: "icon1" do
        %i.fa.fa-edit

  .side__side-contents
    - current_user.groups.each do |group|
      .side__side-contents__groups
        = link_to group_messages_path(group),class:"link" do
          .side__side-contents__group-name
            = group.name
          .side__side-contents__group-message
            %p まだメッセージがありません
